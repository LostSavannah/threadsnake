name: Test

on:

  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  publish:
    runs-on: self-hosted
    steps:
      - name: Test
      - uses: actions/checkout@v3
      - run: |
          echo ready